# GPU Requirements - For Google Colab or GPU environments
# Use this instead of base.txt when running on GPU hardware

-r base.txt

# Replace CPU PyTorch with GPU version
# Note: pip will install GPU version due to URL priority
--index-url https://download.pytorch.org/whl/cu118
torch==2.2.0

# GPU-specific optimizations
bitsandbytes==0.42.0
triton==2.2.0
flash-attn==2.5.2  # Optional: faster attention for training

# For distributed training on GPU clusters (optional)
accelerate==0.26.1
deepspeed==0.12.0  # Optional: large model training
