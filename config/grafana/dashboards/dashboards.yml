# Grafana Dashboard Provisioning
apiVersion: 1

providers:
  - name: 'Banking LLM Dashboards'
    orgId: 1
    folder: 'Banking LLM'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
